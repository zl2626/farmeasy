# Findings

## Existing System

- React/Vite 前端，Django REST Framework 后端。
- 当前 `/` 为启动页，`/home` 首屏为球形图库；用户要求保留。
- 当前数据库作物、政策为 0，前端依赖静态回退。
- 当前行情前端为生成的中国示例数据，后端回退为 2023 印度 CSV，两者不一致。
- 当前素材已有梯田、茶园、稻田、油菜花、竹林、荷塘和乡村市场照片，可作为核验后的中国农业首屏素材候选。

## Confirmed Risks

- 密码重置忽略 token 校验结果。
- Django 密钥和 SMTP 应用密码硬编码；DEBUG 与全域 CORS 开启。
- PDF/图片缺少大小、签名和资源上限校验。
- RAG 只因命中作物名就标记高置信度。
- 手机端球形图库产生水平溢出；视觉克隆进入无障碍树。
- 前端构建可通过但单包约 860KB；Lint 有 40 个错误。
- `npm audit` 有 4 个高危、3 个中危漏洞。
- 后端目录包含约 3.56GB 本地虚拟环境与生成数据，不应进入版本控制。

